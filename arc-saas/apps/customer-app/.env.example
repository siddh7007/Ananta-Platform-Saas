# =============================================================================
# ARC SaaS Customer App - Environment Configuration
# =============================================================================

# API Configuration
VITE_API_URL=http://localhost:3001

# =============================================================================
# Authentication Configuration
# =============================================================================

# Auth Mode: 'keycloak', 'local', or 'both'
# - keycloak: Only show Keycloak SSO login button
# - local: Only show email/password form
# - both: Show both options
VITE_AUTH_MODE=keycloak

# =============================================================================
# Keycloak Configuration
# =============================================================================
# Customer apps authenticate against tenant-specific Keycloak realms.
# Each tenant gets a realm named: tenant-{tenant_key}

# Keycloak server URL
VITE_KEYCLOAK_URL=http://localhost:8180

# =============================================================================
# Tenant-Specific Keycloak Realms
# =============================================================================
#
# When a tenant is provisioned, the system creates:
# - A Keycloak realm: tenant-{tenant_key}
# - A client: {tenant_key}-app
#
# Example for tenant "acme-corp":
# - Realm: tenant-acme-corp
# - Client ID: acme-corp-app
# - Authority URL: http://localhost:8180/realms/tenant-acme-corp
#
# The customer app automatically uses the tenant's realm based on the
# subdomain or tenant context.
#
# =============================================================================
