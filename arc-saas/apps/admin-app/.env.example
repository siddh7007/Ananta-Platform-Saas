# =============================================================================
# ARC SaaS Admin App - Environment Configuration
# =============================================================================
# Copy this file to .env and update values as needed

# =============================================================================
# API Configuration
# =============================================================================

# Backend API URL (tenant-management-service)
# Default: http://localhost:14000 for local development
VITE_API_URL=http://localhost:14000
VITE_API_BASE_URL=http://localhost:14000

# Customer App URL (for redirects after registration)
# Default: http://localhost:27555 for local development
VITE_CUSTOMER_APP_URL=http://localhost:27555

# =============================================================================
# Authentication Configuration
# =============================================================================

# Auth Mode: 'keycloak', 'local', or 'both'
# - keycloak: Only show Keycloak login button (default)
# - local: Only show email/password form
# - both: Show both options
VITE_AUTH_MODE=keycloak

# =============================================================================
# Keycloak Configuration
# =============================================================================
# Keycloak is the default IdP for ARC SaaS
#
# PORT CONFIGURATION:
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ Environment            │ URL                             │ Port    │ Notes │
# ├────────────────────────┼─────────────────────────────────┼─────────┼───────┤
# │ Local dev (Bun/Node)   │ http://localhost:8180           │ 8180    │ *     │
# │ Docker Compose         │ http://localhost:8180           │ 8180    │ *     │
# │ Docker internal        │ http://keycloak:8080            │ 8080    │ **    │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# * Standard port for all local development (both direct and Docker)
# ** Services inside Docker network use internal hostname and port

# Keycloak server URL - Always use 8180 for local development
VITE_KEYCLOAK_URL=http://localhost:8180

# Keycloak realm name
# - Use 'ananta-saas' for the ARC SaaS realm (default)
# - Use 'master' for Keycloak admin access
VITE_KEYCLOAK_REALM=ananta-saas

# Client ID registered in Keycloak for this application
VITE_KEYCLOAK_CLIENT_ID=admin-app

# =============================================================================
# Novu Notification Configuration (Optional)
# =============================================================================

# Novu application identifier (from Novu dashboard)
VITE_NOVU_APP_IDENTIFIER=your-app-identifier

# Novu backend API URL
VITE_NOVU_BACKEND_URL=http://localhost:13100

# Novu WebSocket URL for real-time notifications
VITE_NOVU_SOCKET_URL=http://localhost:13101

# =============================================================================
# Debug/Logging Configuration
# =============================================================================

# Enable detailed API logging in browser console
# Set to 'true' for development debugging, 'false' for production
# Default: 'true' in development, 'false' in production
VITE_ENABLE_API_LOGGING=true

# =============================================================================
# Stripe Configuration (Payment Processing)
# =============================================================================
# Stripe is used for payment method management and billing.
# Get your keys from https://dashboard.stripe.com/apikeys

# Stripe Publishable Key (client-side, safe to expose)
# Use test key for development: pk_test_...
# Use live key for production: pk_live_...
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# =============================================================================
# Grafana Configuration (for Monitoring Dashboards)
# =============================================================================
# Grafana is used to display platform metrics and health dashboards
#
# PORT CONFIGURATION:
# ┌─────────────────────────────────────────────────────────────────────────────┐
# │ Environment            │ URL                             │ Port    │ Notes │
# ├────────────────────────┼─────────────────────────────────┼─────────┼───────┤
# │ Local dev (default)    │ http://localhost:3001           │ 3001    │ *     │
# │ Docker Compose         │ http://localhost:27021          │ 27021   │ **    │
# └─────────────────────────────────────────────────────────────────────────────┘
#
# * Default Grafana port
# ** Docker Compose exposes Grafana on shared-monitoring port

# Grafana server URL for embedding dashboards
VITE_GRAFANA_URL=http://localhost:3001

# Grafana dashboard UIDs (configure based on your dashboards)
# These are the UIDs of pre-configured dashboards in Grafana
VITE_GRAFANA_DASHBOARD_PLATFORM_HEALTH=platform-health
VITE_GRAFANA_DASHBOARD_TENANT_METRICS=tenant-metrics
VITE_GRAFANA_DASHBOARD_API_PERFORMANCE=api-performance

# =============================================================================
# Feature Flags
# =============================================================================
# Toggle features on/off. Set to 'true' to enable, 'false' to disable.
# Most features default to enabled if not specified.

# Billing module (requires billing service integration)
# Default: false (disabled until billing service is configured)
VITE_FEATURE_BILLING=false

# Workflow management module
# Default: true (enabled)
VITE_FEATURE_WORKFLOWS=true

# Monitoring dashboards (health, metrics, analytics)
# Default: true (enabled)
VITE_FEATURE_MONITORING=true

# Audit logs module
# Default: true (enabled)
VITE_FEATURE_AUDIT_LOGS=true

# =============================================================================
# Keycloak Setup Instructions
# =============================================================================
#
# 1. Start Keycloak (from docker-compose.yml):
#    docker-compose up -d keycloak
#
# 2. Access Keycloak Admin Console:
#    - URL: http://localhost:8180
#    - Username: admin
#    - Password: admin
#
# 3. Create a Client for Admin App:
#    - Go to Clients > Create client
#    - Client ID: admin-app
#    - Client type: OpenID Connect
#    - Click Next
#
# 4. Configure Client Settings:
#    - Client authentication: OFF (public client for SPA)
#    - Authorization: OFF
#    - Standard flow: ON
#    - Direct access grants: OFF
#
# 5. Configure Valid Redirect URIs:
#    - Valid redirect URIs: http://localhost:27555/*
#    - Valid post logout redirect URIs: http://localhost:27555
#    - Web origins: http://localhost:27555
#
# 6. Create Admin User (optional):
#    - Go to Users > Add user
#    - Set username, email, first/last name
#    - Under Credentials, set a password
#
# =============================================================================
