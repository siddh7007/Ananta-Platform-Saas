replicaCount: 1
autoscaling:
  enabled: false
ingress:
  hosts:
    - host: cns-dashboard-dev.ananta.local
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: cns-dashboard-dev-tls
      hosts:
        - cns-dashboard-dev.ananta.local
podDisruptionBudget:
  enabled: false
