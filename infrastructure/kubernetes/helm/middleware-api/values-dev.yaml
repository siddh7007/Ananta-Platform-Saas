replicaCount: 1
autoscaling:
  enabled: false
ingress:
  hosts:
    - host: middleware-dev.ananta.local
      paths:
        - path: /
          pathType: Prefix
  tls:
    - secretName: middleware-api-dev-tls
      hosts:
        - middleware-dev.ananta.local
configMap:
  LOG_LEVEL: debug
  NODE_ENV: development
podDisruptionBudget:
  enabled: false
