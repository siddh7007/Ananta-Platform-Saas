apiVersion: v1
kind: Service
metadata:
  name: {{ include "backstage-portal.fullname" . }}
  labels:
    {{- include "backstage-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "backstage-portal.selectorLabels" . | nindent 4 }}
