# Development Dockerfile - Hot Module Reload Enabled
#
# This Dockerfile runs Vite dev server for instant code updates
# No need to rebuild container when code changes

FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code (will be overridden by volume mounts)
COPY . .

# Expose Vite dev server port
EXPOSE 5173

# Start Vite dev server with HMR
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
