# CNS Dashboard Environment Variables
# These are used during build time by Vite
# Copy this file to .env and configure the values

VITE_CNS_PORT=27800
VITE_CNS_DASHBOARD_PORT=27810

# Admin API Token - required for accessing CNS admin endpoints
# Must match ADMIN_API_TOKEN in docker-compose.yml (cns-service environment)
# This token allows CNS staff to access all tenant data for support purposes
# Copy the ADMIN_API_TOKEN value from docker-compose.yml (cns-service environment)
VITE_CNS_ADMIN_TOKEN=your_admin_token_here_must_match_docker_compose

# Gate Logging: Enable [GATE: DataProvider] console logs at critical checkpoints (default: true)
# Shows detailed logs for CRUD operations (getList, create, update, delete, etc.)
# Useful for debugging - can be disabled in production to reduce console noise
VITE_ENABLE_GATE_LOGGING=true

# Supabase - required for Customer Enrichment page and realtime features
# URL: Kong gateway port for Supabase API (default: 27540)
# ANON_KEY: Public anonymous key for read access to enrichment_events table
# Copy from docker-compose.yml customer-portal VITE_SUPABASE_ANON_KEY
VITE_SUPABASE_URL=http://localhost:27540
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
